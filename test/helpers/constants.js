const FOURTY_EIGHT_HOURS_IN_SECONDS = 48 * 60 * 60;

const MARKET_NAMES = {
  ZORA: 'ZORA',
  FOUNDATION: 'FOUNDATION',
};

const MARKETS = [MARKET_NAMES.ZORA, MARKET_NAMES.FOUNDATION];

const NFT_TYPE_ENUM = {
  ZORA: 0,
  FOUNDATION: 1,
};

const PARTY_STATUS = {
  AUCTION_ACTIVE: 0,
  AUCTION_WON: 1,
  AUCTION_LOST: 2,
  NFT_TRANSFERRED: 3,
};

const ONE_ETH = '1000000000000000000';

module.exports = {
  MARKETS,
  MARKET_NAMES,
  NFT_TYPE_ENUM,
  ONE_ETH,
  PARTY_STATUS,
  FOURTY_EIGHT_HOURS_IN_SECONDS,
};
